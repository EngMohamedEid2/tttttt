// import blog from '../blog.json'
// export default defineEventHandler((event) => {
//       const slug = event.context.params.slug;
//       const blogObj = blog.data?.find((elem) => elem?.slug == slug);
//       return  blogObj
//     })
