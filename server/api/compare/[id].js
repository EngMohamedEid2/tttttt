// import compare from './compare.json'
// export default defineEventHandler((event) => {
//     const id = getRouterParam(event, 'id')
//     const compareObj = compare?.data.find((elem) => elem.id == id)
//     return compareObj
//     })
